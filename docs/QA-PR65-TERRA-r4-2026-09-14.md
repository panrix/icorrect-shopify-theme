# QA brief — PR 65 Terra r4 (diagnostic journey + confirmed clocks)

**Model:** `gpt-5.4` (harsh adversarial stand-in — author self-SHIP docs do **not** count)  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed HEAD:** `0c534972e0001feb7c81ec968e5ce08be5da1a54` — abort if `git rev-parse HEAD` differs  
**Prior Terra-reviewed SHA:** `609ef9b546c4dfb32c8a65d64e3f1b47728e679f` (r2 NEEDS-FIX)  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/65  
**This is adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Harsh re-QA of the **delta since r2**. Author-written `docs/QA-PR65-TERRA-r3-RETURN-2026-09-14.md` is **void** (self-review). Judge theme code only.

Ricky confirmed clocks (2026-09-14):
- iPhone known repair: **1** working day
- MacBook / iPad known repair: **2** working days
- Diagnostic quote: **1** working day (device stays; no return promise)
- Watch: **3** (adhesive — unchanged)

Diagnostic UI must **not** say collect → repair → return. It must say collect/send packaging → diagnose & email quote by {date} → customer decides (device stays).

Hub #432 is **merged**. Do not BLOCK solely on hub-not-merged. Still name deploy residual: theme after hub deploy + migration `0028`.

## Forced checklist

1. HEAD is exactly `0c534972e0001feb7c81ec968e5ce08be5da1a54` → YES/NO  
2. `repairBenchDays`: iphone=1, macbook=2, ipad=2, watch=3, diagnostic=1 → YES/NO + evidence  
3. Courier diagnostic journey: no return leg; steps are collect → diagnose & email quote → you decide; `quoteDate` set; `returnDate` null → YES/NO  
4. Mail-in diagnostic journey: send packaging → you post → diagnose & email quote → you decide; no return → YES/NO  
5. ETA for diagnostic uses diagnosis/quote date copy (not “Estimated return by”) → YES/NO  
6. Customer-facing diagnostic copy says “1 working day” / “Quote in 1 working day”, not “24 hours” turnaround → YES/NO  
7. Quote-card stamp still uses DOM dataset (no title in `data-repair="..."`); hostile title test still green → YES/NO  
8. Independent tests: `node --test scripts/courier/*.test.js` → YES/NO + count  
9. `repair_type` / `route` values the theme emits are in hub allowlists (no silent 400s after hub deploy) → YES/NO  

## Prior findings

- r1: attribute XSS + email sync + hub sequencing  
- r2: “24 hours” copy lied vs working-day math (Fri→Mon)  

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`  
2. Checklist table with evidence  
3. P0 / P1 / P2  
4. Deploy residual note  
5. Contradictions / unsafe-to-merge-if-surface-looks-fine  

Write the verdict to `docs/QA-PR65-TERRA-r4-RETURN-2026-09-14.md` in this worktree.
