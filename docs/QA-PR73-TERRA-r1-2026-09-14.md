# QA brief — PR 73 Terra r1 (collection-only quote intro + iPhone/iPad layout)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed code HEAD:** `58202a20c5f1f5f996d76f038687e3df63636250`  
Abort if `git merge-base --is-ancestor 58202a20c5f1f5f996d76f038687e3df63636250 HEAD` fails, or if `git diff 58202a20c5f1f5f996d76f038687e3df63636250 -- sections templates scripts` is non-empty except this brief / your return.  
**PR:** (this branch `cursor/collection-only-copy-d544`)  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Ricky’s Google landing says they are **collection only** because the bench is at capacity and walk-ins are paused. The quote module still opened with “Courier collection across London, or free tracked mail-in nationwide.” People call to book a walk-in.

Also: iPhone and iPad **collection** pages (`collection.iphone-collections.json`, `collection.ipad-collections.json`) showed the quote **below** the model grid. iPad had an empty `details` string.

Judge **this PR’s files only**.

## Forced checklist

1. Code HEAD `58202a2` is an ancestor and theme/script files match that commit → YES/NO
2. Quote wizard heading stays “Get a repair quote” (or the existing contextual auto-title). Subheading now explains walk-ins paused / bench at capacity / collect same day in London or mail-in nationwide → YES/NO
3. Stale courier one-liner is remapped in liquid (`qw_sub_stale`) so leftover theme-editor values still update → YES/NO
4. Homepage `templates/index.json` quote subheading is the new copy; no remaining `Courier collection across London, or free tracked mail-in nationwide.` in templates → YES/NO
5. iPhone collections `order`: `quote_wizard_top` before `parent_categories_nWCAYw` → YES/NO
6. iPad collections `order`: `quote_wizard_top` before `parent_categories_f6HGVL`; `details` is a real iPad description mentioning collection → YES/NO
7. Empty parent-categories `details` no longer render an empty `<p>` → YES/NO
8. `node --test scripts/courier/collection-only-copy.test.js` passes → YES/NO + count
9. `node --test scripts/courier/*.test.js` → YES/NO + count
10. No new P0/P1 (walk-in still implied at quote start, iPad still blank, quote still below iPhone/iPad models, liquid string-break from apostrophes) → YES/NO

## Prior findings

None. First Terra pass on this PR.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR73-TERRA-r1-RETURN-2026-09-14.md` in this worktree.
