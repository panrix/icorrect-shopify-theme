# QA brief — PR 66 quote-events hub gate hotfix

**Round:** r1  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/66  
**Branch:** `cursor/quote-events-hub-gate-165d`  
**Base:** `main`  
**Mode:** harsh Terra — do not rubber-stamp. Do not merge. Do not push.

## Context

Theme #65 shipped repair fields to live `icorrect.co.uk`. Production hub still rejects `repair` with HTTP 400 and drops the whole quote-events beacon. This PR gates outbound repair keys behind `HUB_ACCEPTS_REPAIR_FIELDS = false` so capture resumes. Hub #432 deploy remains a separate VPS step.

## Checklist

1. `HUB_ACCEPTS_REPAIR_FIELDS` defaults to `false` → YES/NO  
2. When false, `quotePayload` never includes `repair`, `repair_handle`, `repair_type`, or `route` → YES/NO  
3. When true (code path), those four keys are still wired from `__icorrectQuote` / dataset stamps → YES/NO  
4. Client-side stamp / `syncIcorrectQuote` paths are unchanged (no attribute XSS regression) → YES/NO  
5. Email / shown / proceeded capture still send through the same gated `quotePayload` → YES/NO  
6. `node --test scripts/courier/hub-quote-events-allowlist.test.js scripts/courier/quoted-repair-stamp.test.js` passes → YES/NO + count  
7. Go-live doc names hub deploy + flip-flag follow-up without claiming hub is live → YES/NO  

## Must not

- Flip the flag to `true` in this PR.  
- Remove client-side repair stamps.  
- “Fix” by weakening hub fail-closed behavior from the theme.

## Verdict line

`SHIP` | `NEEDS-FIX` | `BLOCK` — with one-sentence reason.
