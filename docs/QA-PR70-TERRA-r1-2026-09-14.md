# QA brief — PR 70 Terra r1 (restore power + wireless diagnostic)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed HEAD:** `c0a23e1e76734092c353986e7a547d9b6c8f7b87` — abort if `git rev-parse HEAD` differs  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/70  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Harsh QA of a correction to #69. Ricky confirmed:

- Won’t turn on / won’t charge stay **diagnostic**
- Wi-Fi / Bluetooth / signal stay **diagnostic**
- Water damage is **always** diagnostic
- Named parts (screen, trackpad, keyboard, etc.) stay **repair quotes**

Judge **this PR’s files only**.

## Forced checklist

1. HEAD is exactly `c0a23e1e76734092c353986e7a547d9b6c8f7b87` → YES/NO
2. Labels `Won't turn on`, `Won't charge`, `Won't charge or charger not recognised` are `route: 'diagnostic'` on every device they appear → YES/NO
3. iPhone/iPad/Watch Wi-Fi, Bluetooth, and iPhone signal labels are `route: 'diagnostic'` → YES/NO
4. Every issue in category `Water Damage` is `route: 'diagnostic'` → YES/NO
5. Named parts still `route: 'repair'`: trackpad, cracked/black screen (not “no display, no response”), keyboard, Touch Bar, Face ID, Home button → YES/NO
6. `No display, no response` is diagnostic (dead-phone analogue of won’t turn on) → YES/NO
7. Hub allowlist test still passes → YES/NO
8. `node --test scripts/courier/repair-first-triage.test.js` passes → YES/NO + count
9. `node --test scripts/courier/*.test.js` → YES/NO + count
10. No new P0/P1 (water damage routed off diagnostic; screen/trackpad sent back to diagnostic; allowlist mismatch) → YES/NO

## Prior findings

None. First Terra pass on this PR.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR70-TERRA-r1-RETURN-2026-09-14.md` in this worktree.
