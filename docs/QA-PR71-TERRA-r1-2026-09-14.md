# QA brief — PR 71 Terra r1 (free mail-in ≥£200 at checkout)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed HEAD:** `eefbdf67643d86a31356d3b7d884d8ac30d4340a` — abort if `git rev-parse HEAD` differs  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/71  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Harsh QA of a checkout-trust fix. Leilia #1594 was quoted £479 and paid £499 because `mail-in-service` (£20) was always added to the cart.

Ricky: mail-in is free above £200. Basket must match the all-in quote. Do not put people off.

Judge **this PR’s files only**.

## Forced checklist

1. HEAD is exactly `eefbdf67643d86a31356d3b7d884d8ac30d4340a` → YES/NO
2. `buildCartItems()` in `sections/quote-wizard.liquid` no longer pushes `CFG.mailIn.variantId` → YES/NO
3. Mail-in still sets `Service Type` = `Mail-in` on the repair line → YES/NO
4. Paid postage still adds the adjustment variant only when `_courierQuote.adjustment > 0` → YES/NO
5. `snippets/additional-repair.liquid` no longer adds mail-in-service whenever the mail-in checkbox is checked → YES/NO
6. Pricing: BH10 + £479 mail-in → adjustment 0, total 479 (`courier-pricing.js`) → YES/NO
7. `node --test scripts/courier/mail-in-free-over-200.test.js` passes → YES/NO + count
8. `node --test scripts/courier/*.test.js` → YES/NO + count
9. No new P0/P1 (double postage, free-mail-in jobs still charged £20, under-£200 losing the +£20 adjustment) → YES/NO

## Prior findings

None. First Terra pass on this PR.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR71-TERRA-r1-RETURN-2026-09-14.md` in this worktree.
