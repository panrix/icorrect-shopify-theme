# QA brief — PR 72 Terra r1 (UK pack clock + skip £20 over £200)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed code HEAD:** `fd00f7a02d3697d312c5ea91457290cb55d698a8`  
Abort if `git merge-base --is-ancestor fd00f7a02d3697d312c5ea91457290cb55d698a8 HEAD` fails, or if `git diff fd00f7a02d3697d312c5ea91457290cb55d698a8 -- assets sections snippets scripts` is non-empty. A later commit that only adds this brief / your return is OK.  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/72  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Ricky tried the live quote after #71 and still saw £20 mail-in. Pack send showed **Tuesday** while he was in Bali (browser-local afternoon) even though UK was still Monday morning.

This PR must:

1. Clock pack send and mail-in journeys in **Europe/London**, never `Date#getHours()` of the browser.
2. Pack cutoff = **15:00 UK**. Same working day until then; after that, next working day.
3. UPost / “you post” / device with us = **pack + 1 working day** (was +2). Then existing bench (iPhone 1 / MacBook+iPad 2 / watch 3), then return **+1 working day** after repair done.
4. Courier B1/B2 “Today” stays **14:00 Europe/London**. B3/B4 never Today. Do **not** move courier cutoff to 12pm or 3pm.
5. Cart: do not add Shopify `mail-in-service` SKU. Do not add the mail-in adjustment variant when quoted repair is **≥ £200**. Under £200 must still be able to add the £20 adjustment.

Judge **this PR’s files only**.

## Forced checklist

1. Code HEAD `fd00f7a` is an ancestor of current HEAD and theme/script files match that commit → YES/NO
2. `packShipDate` in `assets/repair-journey.js` uses `Europe/London` wall time and `PACK_CUTOFF_HOUR === 15` → YES/NO
3. Monday 10:00 UK iPhone mail-in: pack Mon, UPost Tue, back Thu (`2026-09-14` / `15` / `17`) → YES/NO
4. Monday 15:30 UK ships Tuesday → YES/NO
5. Bali Monday 15:16 (`2026-09-14T15:16:00+08:00`) still ships Monday UK → YES/NO
6. Mail-in receive is `addWorkingDays(ship, 1)` in both lib and quote-wizard fallback (no extra post day) → YES/NO
7. Courier B1/B2 Today uses London hour `< 14`; B3/B4 never Today → YES/NO
8. `buildCartItems()` does not push `CFG.mailIn.variantId`; skips mail-in adjustment when `currentRepairPrice() >= 200`; still adds adjustment when `adjustment > 0` under £200 → YES/NO
9. `snippets/additional-repair.liquid` has the same ≥£200 mail-in skip and does not add `mail-in-service` → YES/NO
10. `node --test scripts/courier/repair-journey.test.js scripts/courier/courier-slots.test.js scripts/courier/mail-in-free-over-200.test.js` passes → YES/NO + count
11. `node --test scripts/courier/*.test.js` → YES/NO + count
12. No new P0/P1 (Bali timezone rollback, UPost still +2, courier cutoff moved, ≥£200 still charged £20, under-£200 lost +£20) → YES/NO

## Prior findings

None. First Terra pass on this PR.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR72-TERRA-r1-RETURN-2026-09-14.md` in this worktree.
