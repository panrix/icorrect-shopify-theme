# QA brief — PR 73 Terra r3 (delta after r2 heading false-positive)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/73  
**Prior:** r2 NEEDS-FIX on `153d54cb1133b5a89c89a4ec573917f60b9b04ce`  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

Read `docs/QA-PR73-TERRA-r2-RETURN-2026-09-14.md` first.

## r2 P1 — out of scope, do not reopen

The generic heading `"Get a repair quote"` is rewritten to `Get your <subject> quote` on product/collection/page. That logic **already shipped** and is **not part of this PR**. Ricky said the heading is fine as it already works. Do **not** fail this PR for keeping that auto-title.

Homepage (no product/collection) still shows **Get a repair quote**.

## In-scope signed-off work

Subheading is always:

> Service Update: the bench is at capacity, so we've paused walk-ins. Get an instant quote online, and we'll collect your {noun} same day in London, nationwide by courier.

Noun: `device` on homepage / unknown; `iPhone` / `MacBook` / `iPad` / `Apple Watch` on those pages.

iPad `details`: same paragraph, no em dash. The “mail-in pack” wording in that older description is **required to keep**; do not fail it against “nationwide by courier”.

iPhone / iPad collection: wizard above the model grid.

## Forced checklist

1. r2 P1 is not treated as a merge blocker → YES/NO
2. Service Update + noun logic still correct (homepage device; iPhone/MacBook/iPad/Watch named) → YES/NO
3. iPad details has no `—`; wizard still above iPhone/iPad model grids → YES/NO
4. `node --test scripts/courier/collection-only-copy.test.js` → YES/NO + count
5. `node --test scripts/courier/*.test.js` → YES/NO + count
6. No **new** P0/P1 in this PR’s delta → YES/NO

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR73-TERRA-r3-RETURN-2026-09-14.md` in this worktree.
