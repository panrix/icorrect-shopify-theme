# QA brief — PR 72 Terra r2 (London fallback after r1 NEEDS-FIX)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/72  
**Prior Terra-reviewed SHA:** `fd00f7a02d3697d312c5ea91457290cb55d698a8` (r1 NEEDS-FIX)  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

Read `docs/QA-PR72-TERRA-r1-RETURN-2026-09-14.md` first. Judge the **delta** plus whether r1 P1/P2 are actually closed.

## r1 findings to close

**P1:** `quote-wizard.liquid` `packShipDate()` fallback used `Date#getHours()` (browser local). Bali 15:16 → Tuesday pack while UK was Monday morning.

**P2:** Tests only covered `assets/repair-journey.js`, not the no-library fallback.

## Claimed delta

- Fallback now has its own `londonWall()` (`Europe/London`) and uses `wall.hour >= PACK_CUTOFF_HOUR`.
- Source contract: `quote-wizard packShipDate fallback uses UK time` in `scripts/courier/repair-journey.test.js`.

## Forced checklist

1. `git diff fd00f7a02d3697d312c5ea91457290cb55d698a8 -- sections/quote-wizard.liquid scripts/courier/repair-journey.test.js` shows the London fallback + contract test, and no other theme/script surprises in `git diff fd00f7a -- assets sections snippets scripts` → YES/NO
2. Fallback `packShipDate` no longer contains `getHours(` and uses `Europe/London` → YES/NO
3. Shared library clocks unchanged: 15:00 UK pack cutoff, UPost +1, iPhone Mon 10:00 → pack Mon / UPost Tue / back Thu, Bali afternoon still ships Monday, courier B1/B2 Today still 14:00 London → YES/NO
4. Cart still skips `mail-in-service` SKU and mail-in adjustment at ≥£200; under-£200 can still add +£20 → YES/NO
5. `node --test scripts/courier/repair-journey.test.js scripts/courier/courier-slots.test.js scripts/courier/mail-in-free-over-200.test.js` passes → YES/NO + count
6. `node --test scripts/courier/*.test.js` → YES/NO + count
7. r1 P1 and P2 are closed; no new P0/P1 → YES/NO

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR72-TERRA-r2-RETURN-2026-09-14.md` in this worktree.
