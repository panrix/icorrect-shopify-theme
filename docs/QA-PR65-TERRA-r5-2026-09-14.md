# QA brief — PR 65 Terra r5 (delta after r4 NEEDS-FIX)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed HEAD:** `1ff56a3e0063a660eae6c6ef2c7e20b6fc3db8fb` — abort if `git rev-parse HEAD` differs  
**Prior Terra-reviewed SHA:** `0c534972e0001feb7c81ec968e5ce08be5da1a54` (r4 NEEDS-FIX)  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/65  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push.

## Mission

Re-QA **only the delta** since r4. Solution Brief: `docs/SOLUTION-BRIEF-PR65-TERRA-r4-2026-09-14.md`.

r4 P1s claimed fixed:
1. Diagnostic card copy is service-neutral (courier or mail-in).
2. Executable hub #432 allowlist contract test exists and passes.

Do not re-litigate clocks or journey timelines unless the delta regresses them.

## Forced checklist

1. HEAD is exactly `1ff56a3e0063a660eae6c6ef2c7e20b6fc3db8fb` → YES/NO  
2. Liquid no longer contains `We collect, diagnose within 1 working day` → YES/NO  
3. Liquid contains courier-or-mail-in arrival wording on the diagnostic card → YES/NO  
4. `node --test scripts/courier/hub-quote-events-allowlist.test.js` passes → YES/NO + count  
5. Full courier suite still green: `node --test scripts/courier/*.test.js` → YES/NO + count  
6. No new P0/P1 introduced in the delta → YES/NO  

## Prior findings (r4)

P1: mail-in still saw “We collect”. P1: hub allowlist not proved in-repo.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`  
2. Checklist table  
3. P0 / P1 / P2  
4. Deploy residual: theme after hub #432 deploy + migration `0028`  

Write to `docs/QA-PR65-TERRA-r5-RETURN-2026-09-14.md`.
