# QA brief — PR 74 Terra r1 (quote intro = collect offer)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed code HEAD:** `2429fa6bb1baff28edbb635588f6b181afccb58b`  
**PR:** branch `cursor/quote-intro-collect-d544`  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Ricky: the Service Update / “bench is at capacity” line puts people off. Go back to a simple collect offer.

Heading stays **Get a repair quote** (existing auto-title on product/collection pages is pre-existing — do not fail it).

Subheading is always:

> We'll collect your {noun} same day in London, nationwide by courier.

Noun: homepage `device`; iPhone / MacBook / iPad / Apple Watch on those pages.

Must **not** contain: Service Update, bench is at capacity, paused walk-ins.

Keep iPhone/iPad wizard above the model grid. Keep iPad description (no em dash).

## Forced checklist

1. Liquid + homepage/device JSON use the collect line and the right noun → YES/NO
2. No `Service Update` / `bench is at capacity` / `paused walk-ins` in `sections/quote-wizard.liquid` or quote `subheading` JSON → YES/NO
3. iPhone/iPad collection order unchanged (wizard first) → YES/NO
4. `node --test scripts/courier/collection-only-copy.test.js` → YES/NO + count
5. `node --test scripts/courier/*.test.js` → YES/NO + count
6. No new P0/P1 → YES/NO

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR74-TERRA-r1-RETURN-2026-09-14.md`.
