# QA brief — PR 69 Terra r1 (repair-first quote triage)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed HEAD:** `82e7893363b02e6992be29b3955ec49890d988db` — abort if `git rev-parse HEAD` differs  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/69  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Harsh QA of a conversion PR that claims the quote wizard no longer over-routes to diagnostic.

Ricky’s rule: **device + model + fault → repair quote**. Diagnostic is only for liquid damage, data recovery, and camera condensation. If liquid or no signs of life is found after intake, the shop tells the customer then.

Judge **this PR’s files only**. Soft lead-gate, express mail-in, and collection repair FAQs that still say 2–3 working days are out of scope.

## Forced checklist

1. HEAD is exactly `82e7893363b02e6992be29b3955ec49890d988db` → YES/NO
2. In `var TS` inside `sections/quote-wizard.liquid`, every `route: 'diagnostic'` issue is in category `Water Damage` or `Data Recovery`, or label `Condensation in camera` → YES/NO
3. These labels are `route: 'repair'` (not diagnostic): `Won't turn on`, `Won't charge`, `Won't charge or charger not recognised`, `Random shutdowns or reboots`, `Black screen`, `No display, no response`, `Keys not responding`, `Front camera or Face ID not working`, `Display dead or unresponsive` → YES/NO
4. Water Damage / Data Recovery still have diagnostic routes; diagnostic card still contains `Once your device arrives — by courier or mail-in — we diagnose within 1 working day` and the estimate bar (Diagnostic + logic board “from”) → YES/NO
5. Board-level Other (iPhone signal / Wi-Fi / Bluetooth, iPad speakers / Wi-Fi / Bluetooth, Watch Wi-Fi / Bluetooth) is `contact`, not diagnostic → YES/NO
6. Every `repairType` / `route` literal is still in the hub #432 allowlists (`scripts/courier/hub-quote-events-allowlist.test.js`) → YES/NO
7. `node --test scripts/courier/repair-first-triage.test.js` passes → YES/NO + count
8. Full courier suite: `node --test scripts/courier/*.test.js` → YES/NO + count
9. No new P0/P1 in the delta (diagnostic still shown for bookable screen/battery/port faults; missing repairType that breaks checkout; invented express-diagnostic upsell; allowlist mismatch) → YES/NO

## Prior findings

None. First Terra pass on this PR.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR69-TERRA-r1-RETURN-2026-09-14.md` in this worktree.
