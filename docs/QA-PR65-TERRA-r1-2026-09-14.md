# QA brief — PR 65 quote-events repair beacons

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/home/ricky/worktrees/icorrect-shopify-theme-quote-repair`  
**Claimed HEAD:** `7241ea6b8a6d48b652d1d6b6fea0bd417ae6c2f0` — abort if `git rev-parse HEAD` differs  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/65  

## Mission

Adversarial QA of quote-wizard capture: send the quoted Shopify product on quote-events beacons. Do **not** implement. Do not rubber-stamp. Do not merge. Do not push.

Depends on workshop-os PR #432. If this theme ships first, `POST /api/quote-events` 400s on unknown fields and **all** capture dies.

## Forced checklist

1. HEAD is exactly `7241ea6b8a6d48b652d1d6b6fea0bd417ae6c2f0` → YES/NO  
2. `quotePayload` includes `repair`, `repair_handle`, `repair_type`, `route` and still drops null/empty keys → YES/NO  
3. Repair card stamps `data-repair`, `data-repair-handle`, `data-repair-type`, `data-route` from the actual product (title/handle), not a generic fault label → YES/NO  
4. Diagnostic card stamps diagnostic product title/handle and `repair_type=diagnostic` / `route=diagnostic` → YES/NO  
5. `syncIcorrectQuote` copies those dataset fields onto `window.__icorrectQuote` before quote-shown / proceed / email-quote beacons → YES/NO  
6. No new PII (no full postcode, name, phone) on quote-shown/proceed payloads → YES/NO  
7. Titles/handles passed through `esc()` in HTML attributes (no XSS via product.title) → YES/NO  
8. Confirm deploy order risk: this PR must not go live before hub #432 → YES/NO  

## Prior findings

None. First Terra pass.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`  
2. Checklist table  
3. P0 / P1 / P2  
4. Contradictions  
5. Unsafe to merge even if surface looks fine  

Write the verdict to `docs/QA-PR65-TERRA-RETURN-2026-09-14.md` in this worktree.
