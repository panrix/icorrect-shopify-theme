# QA brief — PR 68 Terra r1 (diagnostic FAQ / schema clocks)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**Claimed HEAD:** `c922f15d4aa5f4d6c380393df7704591846fc328` — abort if `git rev-parse HEAD` differs  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/68  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Mission

Harsh QA of a copy/schema PR that claims to align diagnostic FAQ + JSON-LD clocks with the wizard clocks shipped in #65:

- Diagnose + email quote in **1 working day** after the device arrives
- Service-neutral: courier **or** mail-in
- Device stays until the customer decides
- Homepage `FAQPage` JSON-LD matches the visible homepage FAQ answer
- Watch diagnostic template is watch-named, not an iPad paste
- No new express-diagnostic upsell

Judge **this PR’s files only**. Collection **repair** FAQs that still say 2–3 working days are documented out of scope. Do not BOUNCE solely for those leftovers.

## Forced checklist

1. HEAD is exactly `c922f15d4aa5f4d6c380393df7704591846fc328` → YES/NO
2. Homepage FAQ + schema + `homepage-faqs.liquid` + `collection.all-devices.json` no longer contain `two to three working days` / `2-3 working days` / `2 to 3 working days` / `one to two working days` → YES/NO
3. Those same files contain `diagnose within 1 working day`, courier-or-mail-in wording, and device-stays wording → YES/NO
4. Homepage `FAQPage` JSON-LD `acceptedAnswer.text` equals the visible homepage FAQ answer with HTML stripped → YES/NO
5. Diagnostic collection/page files in the PR (`ipad-diagnostics`, `macbook-diagnostic`, `iphone-diagnostics`, `apple-watch-diagnostics`, `watch-diagnostics`, `page.advanced-diagnostics`) no longer contain the stale clock phrases above and do contain `1 working day` → YES/NO
6. `templates/collection.apple-watch-diagnostics.json` contains no `iPad` string → YES/NO
7. `node --test scripts/courier/faq-clock-copy.test.js` passes → YES/NO + count
8. Full courier suite: `node --test scripts/courier/*.test.js` → YES/NO + count
9. No new P0/P1 in the delta (JSON-LD/visible FAQ mismatch, device-name paste, invented express claim, broken JSON) → YES/NO

## Prior findings

None. First Terra pass on this PR.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR68-TERRA-r1-RETURN-2026-09-14.md` in this worktree.
