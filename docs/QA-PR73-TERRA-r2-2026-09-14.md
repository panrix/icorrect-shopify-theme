# QA brief — PR 73 Terra r2 (signed-off Service Update copy)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/workspace`  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/73  
**Claimed code HEAD:** `153d54cb1133b5a89c89a4ec573917f60b9b04ce`  
Abort if theme/script files differ from that commit except this brief / your return.  
**Adversarial QA.** Do **not** implement. Do not merge. Do not push. Do not rubber-stamp.

## Signed-off copy (Ricky)

Heading stays **Get a repair quote**.

> Service Update: the bench is at capacity, so we've paused walk-ins. Get an instant quote online, and we'll collect your {noun} same day in London, nationwide by courier.

Noun: homepage / unknown = `device`. iPhone pages = `iPhone`. MacBook = `MacBook`. iPad = `iPad`. Watch = `Apple Watch`.

iPad collection `details` stays as written, **no em dash**.

iPhone / iPad collection wizards stay **above** the model grid.

## Forced checklist

1. Liquid always builds that Service Update sentence and picks the noun from prefill / metafield / handle / title → YES/NO
2. Homepage JSON uses `device`; iPhone / MacBook / iPad / Watch collection JSON use the matching noun → YES/NO
3. iPad `details` has no `—` and still describes iPad repairs + collection → YES/NO
4. iPhone + iPad collection `order` still has `quote_wizard_top` before the model grid → YES/NO
5. `node --test scripts/courier/collection-only-copy.test.js` → YES/NO + count
6. `node --test scripts/courier/*.test.js` → YES/NO + count
7. No new P0/P1 (homepage saying iPhone, iPhone page saying device, liquid apostrophe break, quote back under the model grid) → YES/NO

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`
2. Checklist table with evidence
3. P0 / P1 / P2
4. Contradictions / unsafe-to-merge-if-surface-looks-fine

Write the verdict to `docs/QA-PR73-TERRA-r2-RETURN-2026-09-14.md` in this worktree.
