# QA brief — PR 65 Terra r2 (dataset stamp + journey clocks)

**Model:** `gpt-5.6-terra`  
**Effort:** high  
**cwd / worktree:** `/home/ricky/worktrees/icorrect-shopify-theme-quote-repair`  
**Claimed HEAD:** `609ef9b546c4dfb32c8a65d64e3f1b47728e679f` — abort if `git rev-parse HEAD` differs  
**Prior reviewed SHA:** `7241ea6b8a6d48b652d1d6b6fea0bd417ae6c2f0`  
**PR:** https://github.com/panrix/icorrect-shopify-theme/pull/65  
**This is adversarial round 2 of 2 (soft cap).** Do **not** implement. Do not merge. Do not push.

## Mission

Re-QA the **delta** since r1. r1 BLOCKED on hub-not-live (sequencing) and P1 attribute XSS + email capture without `syncIcorrectQuote()`. Solution Brief: `docs/SOLUTION-BRIEF-PR65-TERRA-r1.md`.

**Do not BLOCK solely because hub #432 is not merged.** That is a documented deploy-order residual. Fail-closed unknown keys on the hub is correct. Judge the **theme code**. If hub-not-merged is the only P0-class item, verdict is `SHIP` with that residual named.

Also in this delta (Ricky 2026-09-14, same PR): courier/mail-in/diagnostic clocks.
- Collection day is day 1.
- iPhone known repair: 1 working day. MacBook/iPad: 2. Watch: 3.
- Diagnostic: collect → 24h diagnose → quote emailed. Device stays. No return date.
- Mail-in: pack same/next WD (2pm cutoff) → customer posts +1 WD → receive +1 → bench → return +1 (repair only).

## Forced checklist

1. HEAD is exactly `609ef9b546c4dfb32c8a65d64e3f1b47728e679f` → YES/NO  
2. Quote card innerHTML does not interpolate title into `data-repair="..."`; stamp uses DOM dataset → YES/NO + evidence  
3. Hostile title `Repair " onmouseover="xss` does not create a new attribute/handler — covered by test → YES/NO  
4. Email-quote capture goes through `fireQuoteEmailRequested` → `syncIcorrectQuote()` then beacon → YES/NO  
5. Independent tests: `node --test scripts/courier/quoted-repair-stamp.test.js scripts/courier/repair-journey.test.js scripts/courier/quote-wizard-repair-stamp.liquid.test.js` → YES/NO + count  
6. Diagnostic journey has `quoteDate` and `returnDate: null` → YES/NO  
7. MacBook courier Monday collect returns Wednesday (2 WD) → YES/NO  

## Prior findings (r1)

P0: hub not live (sequencing). P1: `esc()` in `data-repair`. P1: email-quote without sync.

## Required output

1. VERDICT: `SHIP` | `NEEDS-FIX` | `BLOCKED` | `BOUNCE`  
2. Checklist table  
3. P0 / P1 / P2  
4. Deploy residual: theme after hub #432 live + migration 0028  

Write the verdict to `docs/QA-PR65-TERRA-r2-RETURN-2026-09-14.md` in this worktree.
